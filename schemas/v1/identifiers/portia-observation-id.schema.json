{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://paper-data-suite.github.io/pds-portia/schemas/v1/identifiers/portia-observation-id.schema.json",
  "title": "Portia Observation Identifier",
  "description": "Opaque canonical identifier for one Observation inside one Portia Event. The obs_ prefix is part of the public identifier contract. The identifier does not encode observer, target, student identity, Actor identity, observed content, provenance, lifecycle, concern, severity, or other sensitive meaning.",
  "type": "string",
  "minLength": 5,
  "maxLength": 128,
  "pattern": "^obs_[A-Za-z0-9][A-Za-z0-9_-]*$"
}
